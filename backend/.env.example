DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=wealth_tracker
PORT=3000
# Set this in development to allow the Vite dev server to reach the API directly.
# Leave unset in production (backend serves frontend on the same origin — CORS not needed).
CORS_ORIGIN=http://localhost:5173
